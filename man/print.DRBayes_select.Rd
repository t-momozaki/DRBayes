% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/drbayes_select.R
\name{print.DRBayes_select}
\alias{print.DRBayes_select}
\title{Print a confounder selection fit}
\usage{
\method{print}{DRBayes_select}(x, digits = 3, ...)
}
\arguments{
\item{x}{An object of class \code{"DRBayes_select"} from
\code{\link{drbayes_select}}.}

\item{digits}{Number of significant digits. Default 3.}

\item{...}{Ignored, present for consistency with the generic.}
}
\value{
\code{x}, invisibly.
}
\description{
Everything \code{\link{print.DRBayes}} shows, followed by which covariates
Step 1 of Algorithm 4 selected, which it left out, and what the sweep was
allowed to move.
}
\examples{
set.seed(1)
n <- 250
dat <- data.frame(X1 = rnorm(n), X2 = rnorm(n), X3 = rnorm(n))
dat$A <- rbinom(n, 1, plogis(dat$X1 + 0.5 * dat$X2))
dat$Y <- 2 * dat$A + dat$X1 + dat$X2 + rnorm(n)
fit <- drbayes_select(Y ~ A + X1 + X2 + X3, A ~ X1 + X2 + X3, dat,
                      mc = 1600, bn = 400, seed = 2, verbose = FALSE,
                      control = drbayes_control(n_steps = 150))
print(fit)

}
\seealso{
\code{\link{drbayes_select}}, \code{\link{print.DRBayes}}
}
