% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methods.R
\name{plot.DRBayes}
\alias{plot.DRBayes}
\title{Plot a posterior coupling fit}
\usage{
\method{plot}{DRBayes}(
  x,
  type = c("density", "rank"),
  model = c("outcome", "ps"),
  parameter = 1L,
  col = c("#2166AC", "#B2182B"),
  lwd = 2,
  main = NULL,
  xlab = "Average treatment effect",
  legend = TRUE,
  ...
)
}
\arguments{
\item{x}{An object of class \code{"DRBayes"} from \code{\link[=drbayes_pc]{drbayes_pc()}}.}

\item{type}{\code{"density"} (default) for the overlaid posterior densities, or
\code{"rank"} for rank plots of the MCMC draws.}

\item{model}{For \code{type = "rank"}, which model to plot, \code{"outcome"} (default)
or \code{"ps"}.}

\item{parameter}{For \code{type = "rank"}, the coefficient to plot, given as a
name or as an index. Defaults to the first.}

\item{col}{Two colours, for \code{pc} and for \code{g.comp} in that order. Used by
\code{type = "density"} only.}

\item{lwd}{Line width for the density curves.}

\item{main}{Plot title. Defaults to a description of the estimand.}

\item{xlab}{Label of the horizontal axis.}

\item{legend}{Set to FALSE to leave the legend off the density plot.}

\item{...}{Further graphical parameters passed to the underlying plot.}
}
\value{
Invisibly, \code{x} for \code{type = "density"}, or the list returned by
\code{\link[=rank_plot]{rank_plot()}} for \code{type = "rank"}.
}
\description{
Posterior densities of the two average treatment effect estimands, or rank
plots of the underlying MCMC draws.
}
\details{
With \code{type = "density"} the posterior of \code{pc} is drawn over the posterior of
\code{g.comp} on a common axis. The gap between the two curves is the tilt: it is
how far the doubly robust moment condition has had to move the g-formula
posterior, and its size is summarised by the tilting parameter \code{lambda}
reported by \code{\link[=print.DRBayes]{print.DRBayes()}}. A gap much wider than the posterior spread
means the outcome model and the propensity score model disagree strongly, and
both should be inspected before either estimate is reported. Curves that lie
on top of one another mean the outcome model already satisfied the moment
condition, and \code{lambda} will be at or near zero.

With \code{type = "rank"} the rank histograms of \code{\link[=rank_plot]{rank_plot()}} are drawn for one
coefficient of one of the two models. These need the per-chain draws of the
model coefficients, which a fit only carries when \code{\link[=drbayes_pc]{drbayes_pc()}} was asked to
keep them.

Base graphics are used throughout, and the graphics parameters are restored
on exit.
}
\examples{
# generate_dataset() leaves the caller's random number stream alone, so
# seed the coupling itself to make the fit below reproducible.
set.seed(1)
data <- generate_dataset(nn = 200, pp = 0, seed = 1)
fit  <- drbayes_pc(YY ~ AA + WW1 + WW2 + WW3 + WW4,
                   AA ~ WW1 + WW2 + WW3 + WW4,
                   data = data,
                   outcome.model = bayes_lm, ps.model = bayes_logit,
                   mc = 2000, bn = 500, verbose = FALSE,
                   control = drbayes_control(n_steps = 200))
plot(fit)

}
\seealso{
\code{\link[=print.DRBayes]{print.DRBayes()}}, \code{\link[=summary.DRBayes]{summary.DRBayes()}}, \code{\link[=rank_plot]{rank_plot()}}
}
