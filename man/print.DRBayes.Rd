% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methods.R
\name{print.DRBayes}
\alias{print.DRBayes}
\title{Print a posterior coupling fit}
\usage{
\method{print}{DRBayes}(x, digits = 3, ...)
}
\arguments{
\item{x}{An object of class \code{"DRBayes"} from \code{\link[=drbayes_pc]{drbayes_pc()}}.}

\item{digits}{Number of significant digits. The table of estimands is shown
to the number of decimal places that gives the posterior standard deviation
this many significant digits, so that a wide credible interval around a
large effect does not round away. Defaults to 3.}

\item{...}{Ignored, present for consistency with the generic.}
}
\value{
\code{x}, invisibly.
}
\description{
A one screen summary of a \code{\link[=drbayes_pc]{drbayes_pc()}} fit: the call, the two models, the
data, the posterior mean and a 95 percent credible interval for both
estimands, the state of the sequential Monte Carlo sweep, and the worst
convergence diagnostics.
}
\details{
See \link{DRBayes-methods} for what separates \code{pc} from \code{g.comp}. Two things in
the output are warnings rather than description. If the sweep did not meet
the moment condition, the \code{pc} draws are not doubly robust and the message
says so; treat them as no better than \code{g.comp}. If a convergence threshold is
breached, the offending R-hat or effective sample size is marked, and the
full table is available through \code{\link[=summary.DRBayes]{summary.DRBayes()}}.
}
\examples{
# generate_dataset() leaves the caller's random number stream alone, so
# seed the coupling itself to make the fit below reproducible.
set.seed(1)
data <- generate_dataset(nn = 200, pp = 0, seed = 1)
fit  <- drbayes_pc(YY ~ AA + WW1 + WW2 + WW3 + WW4,
                   AA ~ WW1 + WW2 + WW3 + WW4,
                   data = data,
                   outcome.model = bayes_lm, ps.model = bayes_logit,
                   mc = 2000, bn = 500, verbose = FALSE,
                   control = drbayes_control(n_steps = 200))
fit

}
\seealso{
\code{\link[=summary.DRBayes]{summary.DRBayes()}}, \code{\link[=plot.DRBayes]{plot.DRBayes()}}
}
